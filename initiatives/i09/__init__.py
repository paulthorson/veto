"""Initiative 09 — Connector reliability & coverage (Veto annual roadmap).

Epics live here as importable, testable modules; exact wiring snippets for
cli.py / webui.py / dashboard.py / server.py live in integration_notes.md
(this initiative never edits those files itself — disjoint file scope).

Subpackages:
  channels/          Epic 3: explicit opt-in communication channels.
  calendar_handoff   Epic 4: confirmed interview-prep + follow-up events.
"""
